# Código del backend (Apps Script): se gestiona con clasp, no se publica en GitHub Pages
backend/

# Configuración local de Claude (vista previa)
.claude/

# Estado local de wrangler (la herramienta de Cloudflare)
.wrangler/
servidor/node_modules/

# Manuales de Cloudflare para el asistente (se instalan con "skills add", no son parte del portal)
.agents/
skills-lock.json

# Herramientas y resultados de las pruebas (se instalan con "npm install")
node_modules/
test-results/
playwright-report/
