{
  "name": "portal-planet",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:servidor && npm run test:portal",
    "test:servidor": "vitest run --config servidor/vitest.config.js",
    "test:portal": "playwright test",
    "dev": "wrangler dev --config servidor/wrangler.toml",
    "deploy": "npm test && wrangler d1 migrations apply portal-planet --remote --config servidor/wrangler.toml && wrangler deploy --config servidor/wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@playwright/test": "^1.63.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.138.0"
  }
}
